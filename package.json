{
  "name": "playcanvas-tomb-raider",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Clean-room reconstruction tooling for the 1996 Tomb Raider PC demo, level 2 portion only.",
  "scripts": {
    "build:tools": "tsc -p tsconfig.tools.json",
    "reference:m0": "npm run build:tools && powershell -NoProfile -ExecutionPolicy Bypass -File tools/reference/run-boot-smoke.ps1",
    "gate:m0": "npm run build:tools && node .work/tool-build/tools/m0/gate-m0.js"
  },
  "engines": {
    "node": "24.18.0",
    "npm": "12.0.1"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "7.0.2"
  }
}
